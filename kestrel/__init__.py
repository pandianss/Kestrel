"""Kestrel — positional, research-backed trading system (single-user).

This package is the deterministic plane (data, research, backtest). The
execution safety core is a separate Rust component (doc 13, D-02). See docs/
for the full design and doc 11 for the open-gap register.
"""
__version__ = "0.0.1"
